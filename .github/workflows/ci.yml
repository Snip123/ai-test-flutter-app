name: CI/CD

on:
  push:
    branches: [main]
    tags: ["v*.*.*"]
  pull_request:
    branches: [main]

jobs:
  ci:
    uses: Snip123/ai-test-platform-standards/.github/workflows/flutter.yml@main
    secrets: inherit
